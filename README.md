# yarn-ts-template

```sh
$ git clone git@github.com:kaiiy/yarn-ts-template.git
$ mv yarn-ts-template <new-repository-name>
$ cd <new-repository-name>
$ rm -r .git
$ git init
$ yarn
$ yarn ts ./src/index.ts 
```